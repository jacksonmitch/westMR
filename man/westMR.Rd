% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/westMR.R
\name{westMR}
\alias{westMR}
\title{Fit a Finite Mixture Regression via the Stepwise WEST Procedure}
\usage{
westMR(
  formula,
  data,
  G_max = 4,
  family = c("gaussian", "poisson", "binomial"),
  task = c("both", "variables", "effects"),
  control = build_control()
)
}
\arguments{
\item{formula}{A formula object (e.g., y ~ x) specifying the model.}

\item{data}{A data.frame containing the variables in the formula.}

\item{G_max}{An integer (>= 2) specifying the maximum number of
groups/components.}

\item{family}{A character string specifying the error distribution.
Defaults to 'gaussian' if not provided.}

\item{task}{A character vector specifying which tasks to perform.
Can include "variables", "effects", or both.}

\item{control}{A control object of class "WMRControl" generated by
\code{\link{build_control}}.}
}
\value{
A list of class \code{westMR} containing the matched \code{call},
the \code{formula} (coerced for binomial responses), \code{family},
\code{task}, the candidate \code{G_values}, the \code{best_fit} (the fit
at the BIC-optimal number of components for the final model), and,
depending on \code{task}, the \code{variable_selection} and/or
\code{effect_determination} results (each \code{NULL} if that task was
not run).
}
\description{
Fits a finite mixture regression model to \code{data} using the WEST
procedure to decide which predictors in \code{formula} belong in the
model, and whether each included predictor's effect is homogeneous or
heterogeneous. Supports Gaussian, Poisson and binomial responses.
Estimation is performed via an EM algorithm, with weighted least squares
(or IRWLS for non-Gaussian families) in the M-step.
}
\examples{
set.seed(1)

# Simulate data
n <- 500
pi <- c(0.2, 0.3, 0.5)
z <- sample(1:3, n, replace = TRUE, prob = pi)
x1 <- rnorm(n)
x2 <- rnorm(n)
x3 <- rnorm(n)
x4 <- rnorm(n)

# True parameters for 3 group mixture model
beta <- rbind(
  g1 = c(intercept = -4, x1 = -3, x2 = 1.5, x3 = 2, x4 = 0),
  g2 = c(intercept = 0, x1 = 1, x2 = 1.5, x3 = -1, x4 = 0),
  g3 = c(intercept = 4, x1 = 3, x2 = 1.5, x3 = 0.5, x4 = 0)
)
sigma <- c(1, 0.5, 2)

# Generate linear predictor and corresponding response y
eta <- beta[z, "intercept"] + beta[z, "x1"] * x1 + beta[z, "x2"] * x2 +
  beta[z, "x3"] * x3 + beta[z, "x4"] * x4
y <- rnorm(n, mean = eta, sd = sigma[z])
dat <- data.frame(y = y, x1 = x1, x2 = x2, x3 = x3, x4 = x4)

# Perform sequential stepwise procedure
westMR(formula = y ~ x1 + x2 + x3 + x4, data = dat, G_max = 3)

}
